apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: tier-app
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
